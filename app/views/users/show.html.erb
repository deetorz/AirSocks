<div class="row">
	<div class="col-md-3">
		<div class="center">
    <%= cl_image_tag @user.photo.attached? ? cl_image_path(@user.photo.key) : image_path(@sock.photo_url) %>

		</div>
		<div class="panel panel-default">
			<div class="panel-heading">Verification</div>
			<div class="panel-body">

        Email <%= @user.email %></br>
				Phone number
			</div>
		</div>
	</div>

	<div class="col-md-9">
		<h2><%= @user.username %></h2>
		<div class="description row-space-3">
			<%= @user.location %>
		</div>
	</div>
</div>
